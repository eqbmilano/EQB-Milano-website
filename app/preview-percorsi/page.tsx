import { PercorsiPreview } from "@/components/PercorsiPreview";

export const metadata = { title: "Preview — A chi è dedicato EQB?" };

export default function PreviewPercorsiPage() {
  return <PercorsiPreview />;
}
