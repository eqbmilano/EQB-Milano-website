import { PreviewInizia } from "@/components/PreviewInizia";

export const metadata = {
  title: "Preview — Inizia",
  robots: { index: false, follow: false },
};

export default function PreviewIniziaPage() {
  return <PreviewInizia />;
}
