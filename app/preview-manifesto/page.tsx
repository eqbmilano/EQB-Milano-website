import { PreviewManifesto } from "@/components/PreviewManifesto";

export const metadata = {
  title: "Preview — Manifesto",
  robots: { index: false, follow: false },
};

export default function PreviewManifestoPage() {
  return <PreviewManifesto />;
}
